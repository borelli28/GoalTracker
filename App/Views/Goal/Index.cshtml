@model IEnumerable<App.Models.Goal>

@{
    ViewData["Title"] = "Goals";
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

<div class="text-center">
    <h1 class="display-4">Goals</h1>
    <div>
        @if (Model == null)
        {
            <h6>No goals found</h6>
        }
        else
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Creation Date</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var goal in Model)
                    {
                        <tr>
                            <td>@goal.Id</td>
                            <td>@goal.Name</td>
                            <td>@goal.Description</td>
                            <td>@goal.CreatedAt</td>
                            <td>
                                <a asp-action="Update" asp-controller="Goal" asp-route-id="@goal.Id">Edit</a> |
                                <a asp-action="Delete" asp-controller="Goal" asp-route-id="@goal.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        <a asp-action="Create" asp-controller="Goal" class="btn btn-primary">Create New</a>
    </div>
</div>
